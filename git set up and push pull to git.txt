git init

git remote  -v //show all remote brances

git remote add org "link : private access @ git..." // to add remote branch

git remote remove org //to remove remote branch

git add . //build before commit
git commit -m "first" // commit it
git push org master // push to github
git pull org master // pull code
git push -f orf master // push all existing files 

git branch -m main // Change branch to main
git branch // get all branches
git branch -a // list all branches
git branch -r // list all branches included by other , includes remote
git checkout -b local_branch_name origin/remote_branch_name        // to checkout a remote branch

git checkout -b main //create main branch
git push -u origin main

git branch -d master // delete master branch

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 -  Private Access key
